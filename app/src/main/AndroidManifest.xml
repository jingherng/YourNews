<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.powjh.yournews">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainApp"
            android:label="@string/title_activity_main_app" />
        <activity android:name=".newsWebView" />
        <activity android:name=".AccountRec" />
        <activity android:name=".searchWebView" />
        <activity android:name=".bmWebView" />
        <activity android:name=".Bookmarks" />
        <activity android:name=".LatestNews" />
        <activity android:name=".latestNewsWebView"></activity>
    </application>

</manifest>